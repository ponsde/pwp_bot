# ============================================================================
# 助手自己的 LLM（Text2SQL、意图分类、回答生成）
# ============================================================================
LLM_API_KEY={your-llm-api-key}
LLM_API_BASE={your-llm-api-base}
LLM_MODEL={your-llm-model}
LLM_TIMEOUT=60

# ============================================================================
# OpenViking —— 研报 RAG 专用，独立一套
# ============================================================================
# OV embedding（RAG 必填）
OV_EMBEDDING_API_KEY={your-ov-embedding-api-key}
OV_EMBEDDING_API_BASE={your-ov-embedding-api-base}
OV_EMBEDDING_MODEL={your-ov-embedding-model}
# 向量维度必须和所选 embedding 模型匹配（bge-m3=1024, text-embedding-3-large=3072 等）
OV_EMBEDDING_DIMENSION={your-ov-embedding-dimension}

# OV VLM（OV 用它做 L0/L1 摘要生成 + 图像/扫描 PDF 解析；必须带识图能力，
# 纯文本 LLM 会在 image_summary 环节失败）
OV_VLM_API_KEY={your-ov-vlm-api-key}
OV_VLM_API_BASE={your-ov-vlm-api-base}
OV_VLM_MODEL={your-ov-vlm-model}

# ============================================================================
# 其他
# ============================================================================
SQLITE_DB_PATH=data/db/finance.db
