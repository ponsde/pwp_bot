{
  "storage": {
    "agfs": {
      "port": 1834
    },
    "workspace": "${OV_WORKSPACE}"
  },
  "embedding": {
    "dense": {
      "api_base": "${LLM_API_BASE}",
      "api_key": "${LLM_API_KEY}",
      "provider": "openai",
      "dimension": 1024,
      "model": "${EMBEDDING_MODEL}"
    }
  },
  "vlm": {
    "api_base": "${LLM_API_BASE}",
    "api_key": "${LLM_API_KEY}",
    "provider": "openai",
    "model": "${LLM_MODEL}"
  },
  "bot": {
    "agents": {
      "model": "openai/${LLM_MODEL}",
      "api_key": "${LLM_API_KEY}",
      "api_base": "${LLM_API_BASE}",
      "max_tool_iterations": 30,
      "memory_window": 30,
      "system_prompt": "你是中药上市公司财报智能问数助手。\n\n## 数据库信息\n- SQLite 数据库，4张表：core_performance_indicators_sheet（核心指标）、balance_sheet（资产负债）、income_sheet（利润）、cash_flow_sheet（现金流量）\n- 每张表共有字段：serial_number, stock_code, stock_abbr, report_period, report_year\n- report_period 格式：2023FY（年报）、2024HY（半年报）、2024Q1/Q3（季报）。注意没有 Q2/Q4，用 HY 和 FY 代替。Q2→HY，Q4→FY\n- 查询范围默认就是 A 股中药上市公司（约 72 家），不要问用户是查 A 股/港股/美股\n- stock_abbr 是中文简称（如 华润三九），查询时用 LIKE %关键词% 模糊匹配更稳\n\n## 工具使用（按优先级）\n1. **MCP_fin_query 永远优先**：输入自然语言，自动生成 SQL、执行、并能生成图表（chart_url）。任何涉及趋势/可视化/绘图/对比的问题都必须用它而不是 MCP_fin_sql。返回 rows=[] 且 content 是澄清句时，把这句话原样返回用户\n2. MCP_fin_sql：只在 MCP_fin_query 成功拿到数据但维度不够时才直接写 SQL（不会产生图表）\n3. MCP_fin_tables：只在不确定字段名时用\n4. openviking_search_resources：研报检索（原因/政策/产品/行业趋势）。sources 里的 paper_path / overview 必须在回答末尾引用\n\n## 意图处理规则\n- 明显模糊（问『利润是多少』但没公司或没期间）→ 反问澄清，不要猜\n- 多轮补充：上一轮你反问后、本轮用户补了信息，必须把两轮拼起来。例如上一轮问『金花股份主营业务收入』、本轮说『2025Q3的』，就查金花股份 2025Q3 主营业务收入，不要再问指标\n- 指标和公司在上下文已给出、只是换期间补的情况下，永不再反问『想查什么指标』\n- 信息齐全（公司+期间+指标）→ 一次 MCP_fin_query 拿结果\n- 归因/原因类（『为什么』『原因』）→ 先 MCP_fin_query 拿数据，再 openviking_search_resources 找研报支撑\n\n## 回答格式（硬性约束）\n- **控制在 200 字内**，除非用户要求 top10 这种列表\n- 优先给结论 + 关键数字，不要大段 Markdown\n- 对比/排名用一个简短表格，不要加标题/粗体堆砌\n- 金额单位：数据库中为万元，回答时明确带单位\n- 引用研报时只写 paper_path 文件名末段\n- 不要反问用户、不要说『我可以帮你』『如果需要』『欢迎继续提问』，直接结束回答\n\n## 文件上传处理\n用户上传文件时消息会带 [uploaded_file: 文件名, temp_file_id: xxx]：\n- PDF 财报 → MCP_fin_import_pdf\n- 其他 PDF/文档 → openviking_add_resource\n- 图片 → 直接描述或回应用户意图\n- 不确定时 → 问用户希望怎么处理"
    },
    "channels": [
      {
        "type": "openapi",
        "api_key": "${BOT_CHANNEL_API_KEY}"
      },
      {
        "type": "bot_api",
        "id": "default",
        "enabled": true,
        "api_key": "${BOT_CHANNEL_API_KEY}"
      }
    ],
    "gateway": {
      "host": "0.0.0.0",
      "port": 18790
    },
    "sandbox": {
      "backend": "direct",
      "mode": "shared"
    },
    "ov_server": {
      "mode": "local",
      "server_url": "http://127.0.0.1:18792"
    },
    "tools": {
      "mcp_servers": {
        "fin": {
          "type": "stdio",
          "command": "${PYTHON_BIN}",
          "args": [
            "${REPO_ROOT}/backend/taidi_mcp_server.py"
          ],
          "env": {
            "SQLITE_DB_PATH": "${SQLITE_DB_PATH_ABS}"
          },
          "tool_timeout": 120,
          "enabled_tools": [
            "*"
          ]
        }
      }
    }
  }
}
